const unsubscribe = mobCore.useMouseWheel(
    ({
        client,
        page,
        preventDefault,
        target,
        type,
        pixelX,
        pixelY,
        spinX,
        spinY,
    }) => {
        // code
    }
);

unsubscribe();
