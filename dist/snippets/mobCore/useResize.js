const unsubscribe = mobCore.useResize(
    ({
        documentHeight,
        horizontalResize,
        scrollY,
        verticalResize,
        windowsHeight,
        windowsWidth,
    }) => {
        // code
    }
);

unsubscribe();
