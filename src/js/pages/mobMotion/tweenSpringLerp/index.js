import { html, staticProps } from '../../../mobjs';

export const mobMotion_tween_spring_lerp = () => {
    return html`<html-content
        ${staticProps({ source: '/data/mobMotion/tweenSpringLerp.json' })}
    ></html-content>`;
};
