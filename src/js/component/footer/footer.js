export const Footer = ({ render }) => {
    return render(`
        <footer class="l-footer">
            <div class="l-footer__container">
                footer
            </div>
        </footer>
    `);
};
