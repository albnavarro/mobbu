// @ts-check

/**
 * @param {string} label
 * @param {number} fps
 *
 * @returns {void}
 */
export const fpsLoadedLog = (label, fps) => {
    console.log(`stagger on ${label} loaded at: ${fps} fps`);
};
